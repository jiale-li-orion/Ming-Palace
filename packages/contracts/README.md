# 契约层说明

体验状态机、桥接接口、错误码与埋点字段的 **唯一权威源** 是：

`D:\dev\Saunter\background\06_体验状态机与桥接契约_v1.md`

本目录将来存放从《06》导出的正式契约文件（如 JSON Schema）。修改契约须走独立 `contract/*` PR，双方确认后合并（见《07》第 6.3 节）。

**周末测试构件**可按《09》有意降级部分能力，但不得静默改名状态或接口后回流为权威。
